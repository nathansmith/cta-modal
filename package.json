{
	"private": true,
	"name": "cta-modal",
	"description": "A call-to-action modal",
	"repository": {
		"type": "git",
		"url": "git@github.com:nathansmith/cta-modal.git"
	},
	"scripts": {
		"format": "prettier --write ./*.{css,html,js,json,md,scss} **/*.{css,html,js,json,md,scss}",
		"prepare": "node ./scripts/prepare.js"
	},
	"devDependencies": {
		"eslint": "^8.6.0",
		"eslint-config-prettier": "^8.3.0",
		"eslint-plugin-prettier": "^4.0.0",
		"husky": "^7.0.4",
		"lint-staged": "^12.1.7",
		"prettier": "^2.3.0"
	},
	"lint-staged": {
		"*.{css,html,js,json,md,scss}": "prettier --write"
	}
}
